# Core (needed to chunk, filter, score-by-agreement, write manifests)
numpy>=1.24
soundfile>=0.12
PyYAML>=6.0

# Recommended
librosa>=0.10          # high-quality resampling 8k->16k

# Forced alignment (pick one backend)
whisperx>=3.1          # backend="whisperx"
# torchaudio>=2.1      # backend="torchaudio" (MMS_FA aligner)

# Second-ASR labeler (your Farsi checkpoint)
torch>=2.1
transformers>=4.40
accelerate>=0.30

# Optional: emit a HuggingFace dataset
datasets>=2.19

# Dev
pytest>=7.0
